# ADR-0006: 单文件零图片 + 程序化音频(沿用并固化)

**Status**: accepted

延续初版架构:游戏为单文件 `game.html`,零图片,纯文本 + CSS 实现全部视觉效果(白底问卷、黑底红字、屏幕抖动均为 CSS);音乐由 Web Audio 实时合成(问卷期低音量环境音 + 答题滴声,跳杀骤停),无任何音频文件;M 键与屏幕按钮均可静音;手机适配(触屏、viewport、防下拉刷新)。单文件自包含保证分享部署(任意静态托管)零配置。
